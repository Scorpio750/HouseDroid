Server / Client RTSP webcam "server"
=====================================

This server is meant to transmit a live
webcam feed from a camera given parameters:

  ./server.sh [device] [url] [port]

and on the client side, just run:

  ./client.sh [port]

